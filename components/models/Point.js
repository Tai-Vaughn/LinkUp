import React from 'react';

//class point, to be obtained from database
class Point{
    constructor({ name, distance, safety }) {
        this.name = name;
        this.distance = distance;
        this.safety = safety;
    }
    return (
        
        );
};
const styles = StyleSheet.create({

});

export default new Point();

